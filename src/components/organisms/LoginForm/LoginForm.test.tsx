import { render, screen } from '@testing-library/react';
import { LoginForm } from './LoginForm';

// describe('LoginForm', () => {
//   it('renders with placeholder text', () => {
//     render(<LoginForm />);
//     expect(screen.getByText('LoginForm placeholder')).toBeInTheDocument();
//   });
// });
